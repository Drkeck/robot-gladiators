function fight() {
    //notifying the player.
    window.alert("The fight has begun!");
    //playerName input + logging it
    var playerName = window.prompt("What is your robot's name?");
    console.log(playerName);
    //test messages.
    console.log("this logs a string, good for leving youself a message");
    //math.
    console.log(10 + 10);
    //combining strings and variables by making an string concatenation.
    console.log("Our robots name is " + playerName);
}
 fight();